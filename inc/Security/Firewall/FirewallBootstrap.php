<?php namespace Bromate\RestApiFirewall\Security\Firewall;

defined( 'ABSPATH' ) || exit;


class FirewallBootstrap
{
    public static function register(): void
    {
        add_filter(...);
        add_filter(...);
        add_filter(...);
    }
}